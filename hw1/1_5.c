//Print out the area and circumference of a circle with a given radius.
void circle(float radius);


//Search a value in an integer array, if found, return the
//index;otherwise return -1.

void search(int* inputArray, int key);


/*Report the most frequent character in a given string with a given length.*/
void mostOccuringChar(char* input);


//4.Compute the product of two matrices.
void productMatrix(int** a, int** b, int rows, int cols);

int main(){
    return 0;
}
